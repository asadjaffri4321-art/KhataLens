# Railway Configuration for KhataLens

[build]
builder = "NIXPACKS"
watchPatterns = ["**/*.py", "**/*.tsx", "**/*.ts"]

[deploy]
startCommand = "uvicorn api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
