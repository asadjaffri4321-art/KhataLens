# Nixpacks configuration for Backend (Python API)

[phases.setup]
nixPkgs = ["python39", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m uvicorn api:app --host 0.0.0.0 --port $PORT"
